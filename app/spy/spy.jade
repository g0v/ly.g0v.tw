.scroll-spy
  ul.spy-menu.ui.vertical.menu
    li.item-section(ng-repeat="t in targets" ng-class="{highlight: t.highlight}")
      a.item(href="#{{t.anchor}}") {{t.heading}}
  .spy-content(ng-transclude)
